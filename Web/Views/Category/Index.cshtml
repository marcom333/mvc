@{
    if(ViewBag.user != null)
        Layout = "~/Views/Shared/_Plantilla.cshtml";
    ViewData["Title"] = "Productos";
}

@using Application.Entities

@model List<Category>

@if(ViewBag.Error!=null){
    <div class="alert alert-danger" role="alert">
        <strong>danger</strong> @ViewBag.Error
    </div>
}

<h1>Conoce nuestras categorías</h1>
<p>
    <a class="btn btn-success" asp-action="Create" asp-controller="Category">Registrar</a>
</p>

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Price</th>
            <th>Options</th>
        </tr>
    </thead>
    <tbody>
        @foreach(Category c in Model){
            <tr>
                <td>@c.Name</td>
                <td>@c.Description</td>
                <td>
                    <a asp-action="Detail" asp-controller="User" asp-route-id="@c.CategoryId" class="btn btn-primary">Consultar</a>
                    <a asp-action="Update" asp-controller="User" asp-route-id="@c.CategoryId" class="btn btn-warning">Editar</a>
                    <form asp-action="Delete" asp-route-id="@c.CategoryId" method="post">
                        <input type="submit" class="btn btn-danger" value="Borrar">
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>